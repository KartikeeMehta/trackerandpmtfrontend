// Small helper to keep desktop app API base centralized.
// Uses Railway deployment by default; adjust if needed.

export const API = {
  base: "https://railwayselfpmtdeployed-production.up.railway.app/backend/api",
};

export default API;
